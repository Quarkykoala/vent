name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.25.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Secret scan check
        run: |
          # Ensure no private keys or committed secrets exist in working tree
          ! grep -r "BEGIN RSA PRIVATE KEY" . --exclude-dir=node_modules --exclude-dir=.git
          ! grep -r "BEGIN PRIVATE KEY" . --exclude-dir=node_modules --exclude-dir=.git
          ! grep -r "rzp_live_" . --exclude-dir=node_modules --exclude-dir=.git

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit & Invariant Tests
        run: pnpm test

      - name: Build
        run: pnpm build
